/* This file is a sample configuration file with a few harmless sample 
   options.   
*/

APT 
{
  // Options for apt-get
  Get 
  {
     Download-Only "false";
  };
  
};

// Options for the downloading routines
Acquire
{
  Retries "0";
};

Dir
{
  State
  {
    Lists "/tmp";
  };

  Cache
  {
    Archives "/tmp";
  };

  Etc
  {
    Parts "/home/user/notroot/etc/apt";
  };
};

// Things that effect the APT dselect method
DSelect 
{
  Clean "auto";   // always|auto|prompt|never
};

DPkg 
{
  // Probably don't want to use force-downgrade..
  Options {"--force-overwrite";}
}

user: user $ ls                                        
apt.conf  bin  etc  include  lib  node_modules  notroot  notroot.tgz  projects  share
user: user $ cat apt.conf
/* This file is a sample configuration file with a few harmless sample 
   options.   
*/

APT 
{
  // Options for apt-get
  Get 
  {
     Download-Only "false";
  };
  
};

// Options for the downloading routines
Acquire
{
  Retries "0";
};

Dir
{
  State
  {
    Lists "/tmp";
  };

  Cache
  {
    Archives "/tmp";
  };

  Etc
  {
    Parts "/home/user/etc/apt";
  };
};

// Things that effect the APT dselect method
DSelect 
{
  Clean "auto";   // always|auto|prompt|never
};

DPkg 
{
  // Probably don't want to use force-downgrade..
  Options {"--force-overwrite";}
}

